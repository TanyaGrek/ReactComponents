import styled from 'styled-components';

export const TabsWrapper = styled.div`
  font-weight: 500;
  font-size: 16px;
  line-height: 20px;
  text-transform: uppercase;
  display: inline-flex;
  gap: 30px;
  flex-grow: 5;
`